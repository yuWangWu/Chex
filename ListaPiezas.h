#pragma once

#include <vector>

#include "Tablero.h"
#include "Piezas.h"
#include "Color.h"


//class ListaPiezas {
//	std::vector<Pieza*> lista;
//
//public:
//	bool agregar(Pieza* _pieza);
//	void inicializarBlanco();
//	void inicializarNegro();
//	void asignarPosMundo(Tablero& _tablero);
//
//	void dibuja() { for (auto p : lista) p->dibuja(); };
//	void mueve() { for (auto p : lista) p->mover(); };
//
//	int size() { return static_cast<int>(lista.size()); };
//	auto begin() { return lista.begin(); };
//	auto end() { return lista.end(); };
//
//	void destruir() { for (auto p : lista) delete p; lista.clear(); };
//};
//
